# requirements.txt

Generate requirements

pip freeze > requirements.txt

Install dependencies

pip install -r requirements.txt

This ensures all project dependencies are installed consistently.

Official Documentation:
https://pip.pypa.io/